/* This file is public domain */
#pragma once
#include "qpils-plumming.h"
#include "qpils-thread.h"
#undef Point

namespace PILS
{
    class QtObject;
    class QtClassBase;
	class CallbackHelperBase;
    class QtClass;
    class QtMethod;
    struct QtClasses;
	class PilsThread;

	struct TypedPtr
	{
        const QtClassBase &class_;
		void *ptr;
		size_t hash() const;
	};

	struct TypedResult : TypedPtr
	{
		const Any *pils;
		~TypedResult()
		{
			if (pils) pils->releaseReference();
		}
	};

    class QtClassBase
	{
	public:
        QtClassBase(int level = 0, const QtClass *const *const lineage = nullptr)
			: level(level), lineage(lineage)
		{}
        virtual const QtName &getName() const = 0;
		virtual void* cast(const TypedPtr &ptr) const = 0;
        virtual const QtReference *newSpecial(const HashedConstant *&link, TypedPtr &object, const QtReference *owner) const;
		virtual void release(void *ptr) const = 0;
		virtual bool recognizing(const void *instance, Recognizer &recognizer) const;
		const int level;
        const QtClass *const *const lineage;
        // static const QtClass *const dummyLineage;
	};

    class QtStaticClass
        : public QtClassBase
	{
	public:
        QtStaticClass(const char *namestring);
        QtStaticClass(const QtName *name);
        const QtName &getName() const  override {return name;}
        void* cast(const TypedPtr &ptr) const override;
        void release(void *ptr) const override;
	private:
        friend class QtClass;
        const QtName &name;
	};

    class QtDummyClass
        : public QtStaticClass
	{
	public:
        QtDummyClass() : QtStaticClass("") {}
        void* cast(const TypedPtr &ptr) const override;
        static const QtDummyClass singleton;
	};

    class QtClass
        : public QtClassBase, QtSpecialConverter
	{
	public:
		struct Mixin;
        QtClass(const char *name, int level, const QtClass *const *lineage, void (*releaseThunk)(void *), int mixinCount, const Mixin *mixinOf)
            : QtClassBase(level, lineage), staticClass(name), releaseThunk(releaseThunk), mixinCount(mixinCount), mixinOf(mixinOf)
		{
			(Converter *&)(getName().typecheck) = this;
		}
        QtClass(const QtName *name, int level, const QtClass *const *lineage, void (*releaseThunk)(void *), int mixinCount, const Mixin *mixinOf)
            : QtClassBase(level, lineage), staticClass(name), releaseThunk(releaseThunk), mixinCount(mixinCount), mixinOf(mixinOf)
		{
			(Converter *&)(getName().typecheck) = this;
		}
        const QtName &getName() const  override {return staticClass.name;}
        void release(void *ptr) const override;
        void* cast(const TypedPtr &ptr) const override;
        virtual const QtReference *wrap(void *object, CallbackHelperBase *helper) const;
		virtual const Constant *ownedReference(void *object) const;
        QtStaticClass staticClass;
        static const QtClasses classes;
        static const QtClass *const lineages[];
		struct Mixin
		{
            Mixin(const QtClass &of, size_t offset)
				: of(of), offset(offset)
			{}
            const QtClass &of;
			const size_t offset;
		};
		static const Mixin mixins[];
        const Constant *reference(const void *) const;
        bool converting(const QtReference &argument) override; // for typechecking
	private:
		void (* const releaseThunk)(void *);
		const int mixinCount;
		const Mixin *const mixinOf;
	};

    class QtReferenceCountedClass
        : public QtClass
	{
	public:
        QtReferenceCountedClass(const char *name, int level, const QtClass *const *lineage, void (* releaseThunk)(void *), int mixinCount, const QtClass::Mixin *mixinOf)
            : QtClass(name, level, lineage, releaseThunk, mixinCount, mixinOf)
		{}
        const QtReference *newSpecial(const HashedConstant *&link, TypedPtr &object, const QtReference *owner) const override;
        const QtReference *wrap(void *object, CallbackHelperBase *helper) const override;
	};

    class QtComponentClass
        : public QtClass
	{
	public:
        QtComponentClass(const char *name, int level, const QtClass *const *lineage, void (* releaseThunk)(void *), int mixinCount, const QtClass::Mixin *mixinOf)
            : QtClass(name, level, lineage, releaseThunk, mixinCount, mixinOf)
		{}
        const QtReference *wrap(void *object, CallbackHelperBase *helper) const override;
        const Constant *ownedReference(void *object) const override;
	};

    class QtReference
		: public ReallySpecial, public TypedPtr
	{
	public:
		virtual void alert();
        const QtReference *const owner;
	protected:
        friend class QtClassBase;
        friend class QtStaticClass;
        QtReference(const HashedConstant *&link, TypedPtr &typedPtr, const QtReference *const owner)
			: ReallySpecial(link), TypedPtr(typedPtr), owner(owner)
		{}
        QtReference(TypedPtr &typedPtr)
			: TypedPtr(typedPtr), owner(NULL)
		{}
        const Any *specialCalling(Runner &run, const QtName &method, const Any &arg) const override;
        const Any *specialCalling(Runner &run, const QtName &method) const override;
        bool specialComparing(const QtLookup &lookup) const override;
        bool converting(QtSpecialConverter &converter) const override;
        bool recognize(Recognizer &recognizer) const override;
		friend class CallbackHelper;
		friend class CallbackHelperBase;
	private:
        size_t unlinkAndGetSize() override;
        void write(Writing &writing) const override;
	};

    class QtOwnedReference
        : public QtReference
	{
	public:
        QtOwnedReference(const HashedConstant *&link, TypedPtr &typedPtr)
            : QtReference(link, typedPtr, NULL)
		{}
	private:
        size_t unlinkAndGetSize() override;
        void write(Writing &writing) const override;
	};

    class QtObject
        : public QtReference
	{
	public:
        QtObject(const HashedConstant *&link, TypedPtr &typedPtr, CallbackHelperBase *helper);
        void alert() override;
		virtual void deleted();
	protected:
		CallbackHelperBase *const helper;
	private:
        size_t unlinkAndGetSize() override;
        void write(Writing &writing) const override;
	};

    class QtComponent
        : public QtObject, PassingMindMap
	{
	public:
        QtComponent(const HashedConstant *&link, TypedPtr &typedPtr, CallbackHelperBase *helper);
        void alert() override;
        void deleted() override;
        const Any *mindError() const override;
        const Step *calling(Runner &run, const Constant &call) const override;
        const Any *specialCalling(Runner &run, const Strap &call) const override;
        bool isMultipleReferenced() const override;
		static volatile long desktopComponentCount;
	private:
        size_t unlinkAndGetSize() override;
		mutable Strap::Sticker strapSticker;
		bool desktop;
	};

    class QtLookup
		: public SpecialLookup
	{
        friend class QtClass;
        friend class QtReference;
        QtLookup(TypedPtr &object)
			: object(object), owner(NULL)
		{}
        const ReallySpecial *newSpecial(const HashedConstant *&link) override;
        bool compare(const ReallySpecial &special) const override;
        void unduplicate() override;
        size_t hash() const override;
		TypedPtr &object;
        mutable const QtReference *owner;
	};

    class QtClassConverter
        : public QtSpecialConverter
	{
	public:
        QtClassConverter(const QtClass &toClass, bool nullable) : toClass(toClass), nullable(nullable) {}
        bool converting(const QtReference &argument) override;
        bool convert(const Cliche &value) override;
		void *value;
	private:
        const QtClass &toClass;
		bool nullable;
	};

	class ArgumentReader
	{
	public:
		bool done() {return at == end;}
	protected:
		ArgumentReader(const Any *argument)
			: argument(argument)
		{
			at = &this->argument;
			if (argument)
			{
				const Integer *count;
				if (argument->isList(at, count))
					end = at + count->value;
				else end = at + 1;
			}
			else end = at;
		}
		const Any *getArgument()
		{
			if (at == end) return NULL;
			else return *at++;
		}
		int remainingArgumentCount()
		{
			return end - at;
		}
	private:
		const Any *argument;
		const Any *const *at;
		const Any *const *end;
	};

	template <typename T> struct ArrayArgument
	{
		ArrayArgument() : value(NULL) {}
		~ArrayArgument() {if (value) delete [] value;}
		T *value;
		void size(int size)
		{
			if (size) value = new T[size];
		}
		operator T*() {return value;}
	};

	class ArgumentSource
		: public ArgumentReader
	{
	public:
		ArgumentSource(const Any *argument)
			: ArgumentReader(argument)
		{}
        template <typename T> bool fill(T *&slot, const QtClass &class_, bool nullable = false)
		{
			return fillClass((void *&)slot, class_, nullable);
		}
        bool fill(std::string &slot);
		bool fill(float &slot);
		bool fill(double &slot);
		bool fill(int &slot);
        bool fill(std::uint32_t &slot);
		bool fill(short &slot);
		bool fill(bool &slot);
        bool fill(std::int64_t &slot);
        bool fill(std::uint64_t &slot);
        bool fill(std::uint16_t &slot);
        bool fill(std::uint8_t &slot);
        // bool fill(unsigned long &slot);

        // bool fill(Array<int> &slot);
        // bool fill(Rectangle<int> &slot);
        // bool fill(juce::Point<int> &slot);
        // bool fill(juce::Point<float> &slot);
        // bool fill(Rectangle<float> &slot);
        // bool fill(juce::Line<float> &slot);
        // bool fill(Range<int> &slot);
        // bool fill(Range<double> &slot);
        // bool fill(var &slot);
        // bool fill(Identifier &slot);
        // bool fill(StringPairArray &slot);
        // bool fill(StringArray &slot);
        // bool fill(const StringArray &slot);
        // bool fill(BigInteger &slot);
        // bool fill(const BigInteger &slot);
		bool fill(char *&slot);
		bool fill(char &slot);
		bool fill(signed char &slot);
		bool fill(const char *&slot);
        bool fill(const void *&slot);
        // bool fill(juce_wchar *&slot);
        // bool fill(const juce_wchar *&slot);
		bool fill(const tm *&slot);
        // bool fill(const OwnedArray<File> &slot);
        // bool fill(const OwnedArray<Font> &slot);
        // bool fill(const ApplicationCommandTarget::InvocationInfo &slot);
        // bool fill(const DirectoryContentsList::FileInfo &slot);
        // bool fill(const DirectoryContentsList::FileInfo *&slot);
        // bool fill(const File &slot);
        // bool fill(const juce::uint8 *&slot);
        bool fill(void *&slot);
        // bool fill(const SparseSet<int> &slot);
        // bool fill(MessageCallbackFunction *&slot);
        // bool fill(Array <PropertyComponent *> &slot);
        // bool fill(Array <const UndoableAction *> &slot);
        bool fill(QtComponent **&slot);
        // bool fill(const File **&slot);
		bool fill(float **&slot);
		bool fill(const Constant *&slot);
		bool fill(const void *&slot, int &count);
		bool fill(const void *&slot, size_t &count);
  //       bool fill(const juce::Path *&slot, const QtClass &class_);
  //       bool fill(const juce::Colour *&slot, const QtClass &class_);
  //       bool fill(juce::Colour *&slot, const QtClass &class_);
        // bool fill(ModalComponentManager::Callback *&slot);
		bool fill(const double *&slot);
        // bool fill(const RelativePoint &dummy);
        // bool fill(const RelativeCoordinate &dummy);
        // bool fill(const RelativeRectangle &dummy);
        // bool fill(juce::BorderSize<int>&slot);

        // bool fill(juce_wchar &slot);

        template <typename T> bool fill(ArrayArgument<T *>&slot, int &count, const QtClass &class_)
		{
			const Any *argument = getArgument();
			if (!argument)
			{
				count = 0;
				return true;
			}
            QtClassConverter converter(class_, true);
			const Any *const *element;
			size_t elementCount;
			if (argument->isList(element, elementCount))
			{
				count = elementCount;
				for (int i = 0; i < count; i++)
				{
					if (element[i]->convert(converter))
					{
						if (i == 0)
							slot.size(count);
						slot.value[i] = (T *)converter.value;
					}
					else return false;
				}
				return true;
			}
			else
			{
				slot.size(count = remainingArgumentCount() + 1);
				for (int i = 0; argument; i++, argument = getArgument())
				{
					if (argument->convert(converter))
						slot.value[i] = (T *)converter.value;
					else return false;
				}
				return true;
			}
		}
	private:
        bool fillClass(void *&slot, const QtClass &class_, bool nullable);
	};

	typedef void (&Thunk)(void *object, void *arguments, void *result);
	typedef bool (&ArgumentsThunk)(void *object, ArgumentSource &source, Thunk thunk, void *result);

    class QtMethod
	{
	public:
        static const Any *invokeChain(const QtMethod *method, Runner &run, const QtReference &object, const Any *operand);
	protected:
        QtMethod(const QtClassBase &class_, const QtName *name)
			: class_(class_), next(name->methods)
		{
            (QtMethod *&)name->methods = this;
		}
        virtual const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const = 0;
	private:
        const QtClassBase &class_;
        const QtMethod *const next;
	};

	template <typename Arguments> bool argsThunk(void *object, ArgumentSource &source, Thunk thunk, void *result)
	{
		Arguments arguments;
		if (arguments.fill(source))
		{
			thunk(object, &arguments, result);
			return true;
		}
		else return false;
	}

	class Method
        : public QtMethod
	{
	protected:
        Method(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
            : QtMethod(class_, QtName::get(name)), argumentsThunk(argumentsThunk), thunk(thunk)
		{}
        Method(const QtClassBase &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk)
            : QtMethod(class_, name), argumentsThunk(argumentsThunk), thunk(thunk)
		{}
		ArgumentsThunk argumentsThunk;
		Thunk thunk;
	};

	class UnimplementedMethod
		: public Method
	{
	public:
        UnimplementedMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class VoidMethod
		: public Method
	{
	public:
        VoidMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        VoidMethod(const QtClassBase &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class PilsReferenceMethod
		: public Method
	{
	public:
        PilsReferenceMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class ReferenceMethod /* creates references without ownership */
		: public Method
	{
	public:
        ReferenceMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk, const QtClass &resultClass)
			: Method(class_, name, argumentsThunk, thunk), resultClass(resultClass)
		{}
        ReferenceMethod(const QtClassBase &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk, const QtClass &resultClass)
			: Method(class_, name, argumentsThunk, thunk), resultClass(resultClass)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	protected:
        const QtClass &resultClass;
	};

	class FactoryMethod /* creates references, with ownership for non-components */
		: public ReferenceMethod
	{
	public:
        FactoryMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk, const QtClass &resultClass)
			: ReferenceMethod(class_, name, argumentsThunk, thunk, resultClass)
		{}
        FactoryMethod(const QtClassBase &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk, const QtClass &resultClass)
			: ReferenceMethod(class_, name, argumentsThunk, thunk, resultClass)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	/* this template doesn't compile when instantiated - CopyMethod classes are expanded by the plumming-generator
	template <typename T> class CopyMethod // copies result objects by new T(T&)
		: public ReferenceMethod
	{
	public:
        CopyMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk, const QtClass &resultClass)
			: ReferenceMethod(class_, name, argumentsThunk, thunk, resultClass)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const
		{
			type result;
			ArgumentSource source(operand);
			if ( (*this->argumentsThunk)(rawObject, source, thunk, &result))
				return resultClass.ownedReference(new T(result));
			else return NULL;
		}
    private:
        typedef T type;
	};
    */

	class ConstructorMethod /* creates PILS-extended objects, with ownership for non-components */
		: public FactoryMethod
	{
	public:
        ConstructorMethod(const QtClass &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk, const QtClass &resultClass)
			: FactoryMethod(class_.staticClass, name, argumentsThunk, thunk, resultClass)
		{}
        ConstructorMethod(const QtClass &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk, const QtClass &resultClass)
			: FactoryMethod(class_.staticClass, name, argumentsThunk, thunk, resultClass)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class IntMethod
		: public Method
	{
	public:
        IntMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        IntMethod(const QtClassBase &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	typedef IntMethod CommandIDMethod;

	class Uint32Method
		: public Method
	{
	public:
        Uint32Method(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        Uint32Method(const QtClassBase &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class ShortMethod
		: public Method
	{
	public:
        ShortMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class Int64Method
		: public Method
	{
	public:
        Int64Method(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class CharMethod
		: public Method
	{
	public:
        CharMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class ConstCharPtrMethod
		: public Method
	{
	public:
        ConstCharPtrMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        ConstCharPtrMethod(const QtClassBase &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class BoolMethod
		: public Method
	{
	public:
        BoolMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        BoolMethod(const QtClassBase &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	typedef CharMethod Uint8Method;

    class Qt_wcharMethod
		: public Method
	{
	public:
        Qt_wcharMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class DoubleMethod
		: public Method
	{
	public:
        DoubleMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class FloatMethod
		: public Method
	{
	public:
        FloatMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class StringMethod
		: public Method
	{
	public:
        StringMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        StringMethod(const QtClassBase &class_, const QtName *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class TimeMethod
		: public Method
	{
	public:
        TimeMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
			: Method(class_, name, argumentsThunk, thunk)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

    class PilsColorMethod
        : public Method
    {
    public:
        PilsColorMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
            : Method(class_, name, argumentsThunk, thunk)
        {}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
    };

    class VarMethod
        : public Method
    {
    public:
        VarMethod(const QtClassBase &class_, const char *name, ArgumentsThunk argumentsThunk, Thunk thunk)
            : Method(class_, name, argumentsThunk, thunk)
        {}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
    };

	class IntrinsicMethod
        : public QtMethod
	{
	public:
		IntrinsicMethod(const char *name)
            : QtMethod(QtDummyClass::singleton, QtName::get(name))
		{}
	};

	class ClassExtracterMethod
		: public IntrinsicMethod
	{
	public:
		ClassExtracterMethod(const char *name)
			: IntrinsicMethod(name)
		{}
        const Any *invoke(Runner &run, const QtReference &object, void *rawObject, const Any *operand) const override;
	};

	class CallbackHelperBase
	{
	public:
		CallbackHelperBase() : thread(NULL) {}
		~CallbackHelperBase();
        QtObject *who;
		PilsThread *thread;
		void helper__visibilityChanged();
	protected:
        // static const Constant *toPils(const juce::String &arg)
        // {
        // 	if(int count = arg.copyToUTF8(NULL, 0x7fffffff))
        // 	{
        // 		std::vector<char> buffer(count);
        // 		arg.copyToUTF8(&buffer[0], count);
        // 		return PilsString::get(&buffer[0], count - 1);
        // 	}
        // 	else return PilsString::get((const char *)NULL, 0);
        // }

		static const Constant *toPils(int arg)
		{
			return Integer::get(arg);
		}

        // static const Constant *toPils(juce::juce_wchar arg)
        // {
        // 	return Integer::get(arg);
        // }

		static const Constant *toPils(float arg)
		{
			return Number::get(arg);
		}

		static const Constant *toPils(double arg)
		{
			return Number::get(arg);
		}

        // static const Constant *toPils(int64 arg)
        // {
        // 	return Number::get((double)arg);
        // }

        // static const Constant *toPils(const ApplicationCommandTarget::InvocationInfo &arg);
        // static const Constant *toPils(const DragAndDropTarget::SourceDetails &arg);

		static bool fromPils(const Any *pils);
		static bool fromPils(const Any *pils, bool &result);
		static bool fromPils(const Any *pils, int &result);
        static bool fromPils(const Any *pils, std::int64_t &result);
		static bool fromPils(const Any *pils, double &result);
		static bool fromPils(const Any *pils, float &result);
        static bool fromPils(const Any *pils, std::string &result);
        // static bool fromPils(const Any *pils, juce::StringArray &result);
        // static bool fromPils(const Any *pils, ReferenceCountedObjectPtr<ReferenceCountedObject> &result, QtClass &class_, bool nullable = false);
        // static bool fromPils(const Any *pils, var &result);

        static const Any *fromPilsPtr(const Any *pils, void *&result, QtClass &class_, bool nullable = false);
        const Any *pilsCall(const Any *call) const;
	};

	class SinkJuceCallback
		: Sink
	{
	public:
		SinkJuceCallback(const Any *&result, const Any *whence)
			: result(result), whence(whence)
		{}
        Sink *kick(Runner &run) override;
        const Step *pass(Runner &run, const Any *thing) override;
        const Step *miss(Runner &run) override;
	private:
		const Any *&result;
		const Any *whence;
	};
}
